import { BackNavigationDirective } from './back-navigation.directive';

describe('BackNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new BackNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
